Scaffolded Vite + React + Tailwind files.

Notes:
- No npm installs were run on CI. Run locally:
  npm install
  npm run dev
  npm run build
- Build outputs to `docs/` so GitHub Pages can serve the site from the repository.
- Legacy files were backed up as index.legacy.html and login.legacy.html
